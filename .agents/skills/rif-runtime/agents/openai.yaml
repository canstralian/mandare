interface:
  display_name: "Rif Runtime"
  short_description: "Repo-specific patterns and workflows for rif-runtime"
  default_prompt: "Use the rif-runtime repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true