interface:
  display_name: "Mandare"
  short_description: "Repo-specific patterns and workflows for mandare"
  default_prompt: "Use the mandare repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true